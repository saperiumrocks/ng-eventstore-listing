export enum FilterOperator {
  range = 'range',
  is = 'is',
  any = 'any',
  contains = 'contains',
  endsWith = 'endsWith',
  startsWith = 'startsWith',
  arrayContains = 'arrayContains',
  exists = 'exists',
  notExists = 'notExists'
}
